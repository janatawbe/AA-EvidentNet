# AA-EvidentNet requirements.
#
# Versions below reflect what is verified working in the current dev
# environment (Python 3.10.11, Windows, CPU-only torch build). If you have
# a CUDA-capable GPU, install a CUDA build of torch instead — see
# https://pytorch.org/get-started/locally/ and REPRODUCIBILITY.md.

torch==2.10.0          # CPU-only wheel in the reference environment (torch==2.10.0+cpu)
torchvision==0.25.0    # pinned to match torch==2.10.0; do not `pip install torchvision` unpinned - it will pull a newer torch
timm==1.0.28           # ResNet50/EfficientNetB0/MaxViT backbones with ImageNet-pretrained weights
numpy==2.2.6
pandas==2.3.3
pillow==12.1.1
PyYAML==6.0.3
scikit-learn==1.7.2

# Dev / testing
pytest==9.1.1

# matplotlib==3.10.9: now actually required by
# src/evaluation/ood_uncertainty.py (the severity-vs-score figure) -
# installed and pinned here (was previously listed unpinned as
# "anticipated for later tasks" without ever being installed in the
# reference environment). tqdm remains anticipated/unused.
matplotlib==3.10.9
tqdm
